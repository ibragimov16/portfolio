version: '3.1'

services:
  portfolio:
    build:
      context: ./
      dockerfile: ./Dockerfile
    container_name: portfolio
    ports:
      - '3000:80'
    stdin_open: true
    tty: true
    restart: always
